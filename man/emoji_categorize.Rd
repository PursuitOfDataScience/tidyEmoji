% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emoji-categorize.R
\name{emoji_categorize}
\alias{emoji_categorize}
\title{Categorise each row by the emoji categories it contains}
\usage{
emoji_categorize(data, text)
}
\arguments{
\item{data}{A data frame or tibble containing a text column. Grouped data
frames are accepted. The verbs that work a row at a time (adding columns,
or keeping and expanding rows) carry the grouping through to their result,
as \code{\link[dplyr:mutate]{dplyr::mutate()}} and \code{\link[dplyr:filter]{dplyr::filter()}} do. The verbs that pool across
rows -- the counts, the co-occurrence edge lists, the time series -- warn
that they ignore the grouping and return one corpus-wide answer.}

\item{text}{The text column to scan, supplied unquoted. Any atomic column
is accepted and read as character, so a \code{factor} works and a numeric,
\code{Date} or logical one simply contains no emoji. A list column -- or a
data-frame column -- is refused rather than coerced, because coercing one
deparses it and the emoji found would be in the code rather than in your
data. What counts as an emoji is the same in every verb; see the
\emph{Detection} section of \link{tidyEmoji} for the one case that surprises
people, code points that are emoji only when they carry \code{U+FE0F}.}
}
\value{
\code{data}, as a tibble, filtered to the rows containing at least one
emoji, with an added \code{.emoji_category} column. That column is \code{NA} for a
row whose emoji are all absent from the reference table.
}
\description{
\code{emoji_categorize()} keeps the rows of \code{data} that contain emoji and adds a
\code{.emoji_category} column listing the distinct Unicode categories present in
that row (for example "Smileys & Emotion"), separated by \code{|} when a row spans
more than one category.
}
\details{
A row is kept because it contains an emoji, not because that emoji could be
categorised. If none of a row's emoji is in the reference table the row is
kept with \code{.emoji_category} set to \code{NA}. Dropping it would silently shrink
the corpus, and by exactly the rows a user whose Unicode coverage is behind
most needs to see.

Three different things reach that \code{NA}, and only the first is a catalogue
question:
\enumerate{
\item A zero-width-joiner sequence \strong{newer than your installed \pkg{emoji}
package}, since detection is grapheme-aware and does not require the
sequence to be catalogued. Use \code{\link[=emoji_provenance]{emoji_provenance()}} to check which
catalogue you are matching against; upgrading \code{emoji} fixes it.
\item An \strong{invalid regional-indicator pair} such as \verb{U+1F1FD U+1F1FD}. It is
a well-formed grapheme cluster and no catalogue will ever contain it, so
no upgrade helps.
\item A \strong{non-RGI ZWJ join} -- components joined in a combination Unicode
does not recommend. These arrive as separate occurrences rather than one.
}

See the Detection section of \link{tidyEmoji} for both false-positive cases.
}
\examples{
df <- data.frame(text = c("smile \U0001f600",
                          "flag \U0001f3c1\U0001f600",
                          "nothing"))
emoji_categorize(df, text)
}
\seealso{
\code{\link[=emoji_type]{emoji_type()}} for the smaller functional vocabulary recoded from
the same group and subgroup, and \code{\link[=emoji_faceness]{emoji_faceness()}} for the face share it
reduces to; \code{\link[=emoji_frequency]{emoji_frequency()}} for per-glyph counts carrying \code{group};
\link{category_unicode_crosswalk} for the ten categories themselves.
}
