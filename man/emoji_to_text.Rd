% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emoji-translate.R
\name{emoji_to_text}
\alias{emoji_to_text}
\title{Replace emoji in a text column with words (demojize)}
\usage{
emoji_to_text(data, text, format = c("name", "shortcode"), wrap = ":{x}:")
}
\arguments{
\item{data}{A data frame or tibble containing a text column.}

\item{text}{The text column to scan, supplied unquoted.}

\item{format}{Output form: \code{"name"} (the Unicode name, e.g.
"grinning face") or \code{"shortcode"} (the canonical GitHub-style alias, e.g.
"grinning", wrapped as ":grinning:"). Default \code{"name"}.}

\item{wrap}{When \code{format = "shortcode"}, the wrapper applied to each
shortcode, written as a template with \code{{x}} standing for the shortcode.
Default \code{":{x}:"}. Ignored for \code{format = "name"}.}
}
\value{
\code{data}, as a tibble, with the text column rewritten in place (same
column name). \code{NA} entries stay \code{NA}, and emoji with no known name are left
in place unchanged.
}
\description{
\code{emoji_to_text()} returns a copy of \code{data} with its text column rewritten so
that every emoji is replaced by its name or shortcode. This is useful for
accessibility (screen readers) and as an NLP normalisation step before
tokenising. Detection is grapheme-aware and joins go through \code{emoji_key()},
so emoji carrying the \code{U+FE0F} variation selector still resolve.
}
\examples{
df <- data.frame(text = "great \U0001f600 love \u2764\ufe0f")
emoji_to_text(df, text, format = "name")
emoji_to_text(df, text, format = "shortcode")
}
\seealso{
\code{\link[=text_to_emoji]{text_to_emoji()}} for the inverse (emojize); \code{\link[=as_emoji_name]{as_emoji_name()}},
\code{\link[=as_emoji_shortcode]{as_emoji_shortcode()}}, \code{\link[=as_emoji]{as_emoji()}} for vector helpers.
}
