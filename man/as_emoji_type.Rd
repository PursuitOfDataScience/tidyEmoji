% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emoji-type.R
\name{as_emoji_type}
\alias{as_emoji_type}
\title{Functional type of an emoji glyph}
\usage{
as_emoji_type(x)
}
\arguments{
\item{x}{A character vector of emoji glyphs.}
}
\value{
A character vector the same length as \code{x}.
}
\description{
\code{as_emoji_type(x)} maps emoji glyphs to a small functional vocabulary --
\code{"face"}, \code{"gesture"}, \code{"person"}, \code{"nature"}, \code{"food"}, \code{"place"},
\code{"activity"}, \code{"object"}, \code{"symbol"}, \code{"flag"}, \code{"component"} -- recoded from
the Unicode group and subgroup. The distinction that matters most in the
literature is \code{face} (emotional) against \code{object} (semantic).
}
\details{
The recode is: faces and costumed characters in \emph{Smileys & Emotion} become
\code{face} and the rest of that group (hearts, the anger symbol, ...) becomes
\code{symbol}; hands and gesturing people in \emph{People & Body} become \code{gesture} and
the rest \code{person}; the remaining Unicode groups map one-to-one. Glyphs the
reference table does not know return \code{NA}.
}
\examples{
as_emoji_type(c("\U0001f600", "\U0001f44d", "\U0001f355", "\u2764\ufe0f"))
}
\seealso{
\code{\link[=emoji_type]{emoji_type()}} for the data-frame verb, \code{\link[=emoji_faceness]{emoji_faceness()}} for the
per-row share, \code{\link[=emoji_categorize]{emoji_categorize()}} for the raw Unicode categories.
}
