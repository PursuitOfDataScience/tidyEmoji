% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emoji-ambiguity.R
\name{emoji_flag_ambiguous}
\alias{emoji_flag_ambiguous}
\title{Which emoji in this corpus are most likely to be misread?}
\usage{
emoji_flag_ambiguous(data, text, top_n = 10, measure = "entropy")
}
\arguments{
\item{data}{A data frame or tibble containing a text column. Grouped data
frames are accepted. The verbs that work a row at a time (adding columns,
or keeping and expanding rows) carry the grouping through to their result,
as \code{\link[dplyr:mutate]{dplyr::mutate()}} and \code{\link[dplyr:filter]{dplyr::filter()}} do. The verbs that pool across
rows -- the counts, the co-occurrence edge lists, the time series -- warn
that they ignore the grouping and return one corpus-wide answer.}

\item{text}{The text column to scan, supplied unquoted. What counts as an
emoji is the same in every verb; see the \emph{Detection} section of
\link{tidyEmoji} for the one case that surprises people, code points that
are emoji only when they carry \code{U+FE0F}.}

\item{top_n}{Number of emoji to return, most ambiguous first. \code{NULL} returns
all of them.}

\item{measure}{Ambiguity statistic to rank by; see \code{\link[=emoji_ambiguity]{emoji_ambiguity()}}.}
}
\value{
A tibble with columns \code{emoji}, \code{name}, \code{n} (occurrences in the
corpus), \code{n_annotations}, \code{ambiguity} and \code{rank} (the glyph's rank in the
whole lexicon). Emoji absent from the lexicon cannot be ranked and are
dropped.
}
\description{
\code{emoji_flag_ambiguous()} crosses the emoji actually present in a text column
with their annotation-disagreement statistics and returns the most ambiguous
ones first. It is the content-QA shortlist: the glyphs worth a second look
before a campaign ships or a coding scheme is fixed.
}
\examples{
df <- data.frame(text = c("ok \U0001f643", "yay \U0001f600 \U0001f643",
                          "hmm \U0001f612"))
emoji_flag_ambiguous(df, text, top_n = 3)
}
\seealso{
\code{\link[=emoji_ambiguity]{emoji_ambiguity()}}, \code{\link[=emoji_risk]{emoji_risk()}}.
}
