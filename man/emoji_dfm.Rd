% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emoji-dfm.R
\name{emoji_dfm}
\alias{emoji_dfm}
\title{Document-by-emoji feature matrix}
\usage{
emoji_dfm(data, text, doc_id = NULL, weighting = c("count", "binary", "tfidf"))
}
\arguments{
\item{data}{A data frame or tibble containing a text column.}

\item{text}{The text column to scan, supplied unquoted.}

\item{doc_id}{Optional unquoted column identifying documents; rows sharing
a value are aggregated into one document. Default: each row is a
document.}

\item{weighting}{One of \code{"count"} (default), \code{"binary"} or \code{"tfidf"}.}
}
\value{
A tibble with one row per document: \code{.row_number} (or the \code{doc_id}
column) followed by one numeric column per emoji. Zero emoji in the
corpus yields just the document column.
}
\description{
\code{emoji_dfm()} turns a text column into a wide, model-ready table with one
row per document and one column per emoji, weighted by raw counts, binary
presence or tf-idf. All documents are kept, including those with no emoji
(all-zero rows), so the result aligns row-for-row with the corpus and can be
bound to outcome columns for tidymodels-style workflows.
}
\details{
By default every row of \code{data} is a document and the first output column,
\code{.row_number}, is its position in \code{data} (matching
\code{\link[=emoji_extract_unnest]{emoji_extract_unnest()}}). Give \code{doc_id} to aggregate rows sharing an id
into one document; the id column keeps its name. Emoji columns are named by
the glyph itself, canonicalised through the package's codepoint key (so
qualified and unqualified forms count as one feature), and ordered by
descending total count (ties broken by glyph).

For \code{weighting = "tfidf"}, the cell for emoji \emph{e} in document \emph{d} is
\code{count(d, e) * log(N / df(e))}, where \code{N} is the number of documents and
\code{df(e)} the number of documents containing \emph{e}. An emoji that appears in
every document therefore scores 0.
}
\examples{
df <- data.frame(text = c("\U0001f600\U0001f600 fun", "\U0001f621",
                          "no emoji"))
emoji_dfm(df, text)
emoji_dfm(df, text, weighting = "binary")
emoji_dfm(df, text, weighting = "tfidf")
}
\seealso{
\code{\link[=emoji_frequency]{emoji_frequency()}} for corpus totals; \code{\link[=emoji_tokens]{emoji_tokens()}} for the
long form this widens.
}
