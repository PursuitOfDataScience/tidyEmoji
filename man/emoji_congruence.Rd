% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emoji-incongruity.R
\name{emoji_congruence}
\alias{emoji_congruence}
\title{Text-emoji congruence}
\usage{
emoji_congruence(
  data,
  text,
  text_score,
  method = c("difference", "sign_flip"),
  scale,
  where = c("all", "final"),
  threshold = 1
)
}
\arguments{
\item{data}{A data frame or tibble containing a text column. Grouped data
frames are accepted. The verbs that work a row at a time (adding columns,
or keeping and expanding rows) carry the grouping through to their result,
as \code{\link[dplyr:mutate]{dplyr::mutate()}} and \code{\link[dplyr:filter]{dplyr::filter()}} do. The verbs that pool across
rows -- the counts, the co-occurrence edge lists, the time series -- warn
that they ignore the grouping and return one corpus-wide answer.}

\item{text}{The text column to scan, supplied unquoted. What counts as an
emoji is the same in every verb; see the \emph{Detection} section of
\link{tidyEmoji} for the one case that surprises people, code points that
are emoji only when they carry \code{U+FE0F}.}

\item{text_score}{Unquoted numeric column holding the text's own sentiment.}

\item{method}{\code{"difference"} (default) for the continuous gap, or
\code{"sign_flip"} for the categorical polarity-flip feature.}

\item{scale}{How to make the two scores comparable: \code{"rank"}, \code{"zscore"} or
\code{"none"}. Required -- there is no sensible default.}

\item{where}{\code{"all"} (default) scores every emoji in the row; \code{"final"}
scores only the trailing run of emoji that ends the text.}

\item{threshold}{For \code{method = "difference"}, the absolute gap at or above
which \code{.emoji_incongruent} is \code{TRUE}. Default \code{1}, a full polarity swing on
the rank scale.}
}
\value{
\code{data}, as a tibble, with everything \code{\link[=emoji_incongruity]{emoji_incongruity()}} adds plus
\code{.emoji_congruent}, the negation of \code{.emoji_incongruent}.
}
\description{
\code{emoji_congruence()} is \code{\link[=emoji_incongruity]{emoji_incongruity()}} under the framing used in the
marketing and eWOM literature, where the finding is that a \emph{mismatch}
between a review's words and its emoji lowers perceived helpfulness and
authenticity. Same engine, same columns, plus \code{.emoji_congruent}.
}
\examples{
df <- data.frame(
  text = c("lovely stay \U0001f600", "terrible room \U0001f600"),
  score = c(0.8, -0.9)
)
emoji_congruence(df, text, score, scale = "none")
}
\seealso{
\code{\link[=emoji_incongruity]{emoji_incongruity()}}.
}
