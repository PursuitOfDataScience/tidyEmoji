% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emoji-type.R
\name{emoji_faceness}
\alias{emoji_faceness}
\title{How face-heavy is each row's emoji use?}
\usage{
emoji_faceness(data, text)
}
\arguments{
\item{data}{A data frame or tibble containing a text column.}

\item{text}{The text column to scan, supplied unquoted.}
}
\value{
\code{data}, as a tibble, with added columns \code{.emoji_n},
\code{.emoji_n_typed} (emoji whose type is known), \code{.emoji_n_face} and
\code{.emoji_faceness} (\code{.emoji_n_face / .emoji_n_typed}). Rows with no emoji
get \code{NA}.
}
\description{
\code{emoji_faceness()} reports the share of a row's emoji that are faces. Face
emoji act as emotional signals and object emoji as semantic ones, and the
two have measurably different effects on engagement, so the split is worth a
column of its own.
}
\examples{
df <- data.frame(text = c("\U0001f600\U0001f355", "\U0001f600", "none"))
emoji_faceness(df, text)
}
\seealso{
\code{\link[=emoji_type]{emoji_type()}}, \code{\link[=as_emoji_type]{as_emoji_type()}}.
}
