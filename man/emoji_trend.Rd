% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emoji-time.R
\name{emoji_trend}
\alias{emoji_trend}
\title{Emoji frequency over time}
\usage{
emoji_trend(
  data,
  text,
  time,
  by = "month",
  top_n = 20,
  measure = c("n", "share")
)
}
\arguments{
\item{data}{A data frame or tibble containing a text column. Grouped data
frames are accepted. The verbs that work a row at a time (adding columns,
or keeping and expanding rows) carry the grouping through to their result,
as \code{\link[dplyr:mutate]{dplyr::mutate()}} and \code{\link[dplyr:filter]{dplyr::filter()}} do. The verbs that pool across
rows -- the counts, the co-occurrence edge lists, the time series -- warn
that they ignore the grouping and return one corpus-wide answer.}

\item{text}{The text column to scan, supplied unquoted. Any atomic column
is accepted and read as character, so a \code{factor} works and a numeric,
\code{Date} or logical one simply contains no emoji. A list column -- or a
data-frame column -- is refused rather than coerced, because coercing one
deparses it and the emoji found would be in the code rather than in your
data. What counts as an emoji is the same in every verb; see the
\emph{Detection} section of \link{tidyEmoji} for the one case that surprises
people, code points that are emoji only when they carry \code{U+FE0F}.}

\item{time}{Unquoted column of dates or date-times (\code{Date}, \code{POSIXct}, or
character in \code{"YYYY-MM-DD"} form). A date-time is bucketed by the calendar
day it \emph{displays} as in its own timezone, not by its UTC day: an emoji
posted at 23:30 New York time belongs to that day, not to the next one.

"Its own timezone" means the column's \code{tzone} attribute. A \code{POSIXct}
created without one -- which is what \code{as.POSIXct("2024-01-01 23:30")} and
most CSV readers give you -- has no timezone of its own, so R displays it
in the session's, and the buckets follow. The same column then gives hour
23 on one machine and hour 4 on another. Tag the column
(\code{as.POSIXct(x, tz = "UTC")}, or \code{lubridate::force_tz()}) if the result
has to be reproducible; a \code{Date} column is immune either way.

A character column must lead with a four-digit year: \code{"2024-01-01"} or
\code{"2024/01/01"}, with one- or two-digit month and day, and any trailing
time ignored. Values that do not parse warn and are dropped -- but a column in
which \emph{nothing} reads as a date is an error rather than a column of \code{NA},
since there would be no time axis left. Note that \code{"01/02/2024"} is in the
second group: convert a column written that way with \code{as.Date()} and its
own \code{format} first.}

\item{by}{Period length: \code{"day"}, \code{"week"} (starting Monday), \code{"month"}
(default), \code{"quarter"} or \code{"year"}.}

\item{top_n}{Number of emoji to follow, ranked by \code{measure} over the whole
corpus. \code{NULL} keeps every emoji. Default \code{20}.}

\item{measure}{Statistic used to rank emoji for \code{top_n} and to order the rows
within a period: \code{"n"} (default) or \code{"share"}.}
}
\value{
A tibble with columns \code{.period} (a \code{Date}, the start of the period),
\code{emoji}, \code{name}, \code{n} and \code{share}.
}
\description{
\code{emoji_trend()} counts emoji per time period and returns the long table
that plots directly: one row per (period, emoji) over the periods it
returns, including the ones in which a given emoji is absent, so a trend
line does not silently skip its zeros.
}
\details{
\strong{Which periods appear.} The grid is complete over the \emph{observed} periods,
and "observed" means a period holding at least one emoji. A period whose
rows carry no emoji at all does not appear, and neither does a gap in the
calendar: \code{emoji_trend()} never invents a period. So the zeros it fills in
are the ones \emph{within} the periods it returns, not a continuous time axis.
Pass the result through \code{\link[tidyr:complete]{tidyr::complete()}} against a calendar sequence if
you need the empty periods too.

The three time verbs answer this differently, on purpose, and it is worth
knowing which you are getting before joining two of them on \code{.period}:
\itemize{
\item \code{\link[=emoji_trend]{emoji_trend()}} -- periods containing at least one emoji.
\item \code{\link[=emoji_turnover]{emoji_turnover()}} -- every period containing at least one dated row,
including emoji-free ones, which report \code{n_types = 0}.
\item \code{\link[=emoji_seasonality]{emoji_seasonality()}} -- every level of the cycle unconditionally, whether
or not the data reaches it.
\code{share} is the emoji's count divided by all emoji tokens in the same period,
which is what makes periods with different volumes comparable. \code{top_n}
selects the emoji to follow, ranked over the whole corpus by \code{measure}, and
the selected set is the same in every period.
}

Rows whose time is missing or unparseable contribute nothing. Glyphs are
canonicalised through the package's codepoint key, so qualified and
unqualified forms share one series.
}
\examples{
df <- data.frame(
  when = as.Date(c("2024-01-05", "2024-01-20", "2024-02-03")),
  text = c("\U0001f600 hi", "\U0001f600\U0001f602", "\U0001f602 yes")
)
emoji_trend(df, text, when)
}
\seealso{
\code{\link[=emoji_turnover]{emoji_turnover()}} for vocabulary churn, \code{\link[=emoji_seasonality]{emoji_seasonality()}} for
cyclical patterns.
}
