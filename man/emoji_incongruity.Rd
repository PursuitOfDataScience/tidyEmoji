% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emoji-incongruity.R
\name{emoji_incongruity}
\alias{emoji_incongruity}
\title{Text-emoji sentiment mismatch}
\usage{
emoji_incongruity(
  data,
  text,
  text_score,
  method = c("difference", "sign_flip"),
  scale,
  where = c("all", "final"),
  threshold = 1
)
}
\arguments{
\item{data}{A data frame or tibble containing a text column. Grouped data
frames are accepted. The verbs that work a row at a time (adding columns,
or keeping and expanding rows) carry the grouping through to their result,
as \code{\link[dplyr:mutate]{dplyr::mutate()}} and \code{\link[dplyr:filter]{dplyr::filter()}} do. The verbs that pool across
rows -- the counts, the co-occurrence edge lists, the time series -- warn
that they ignore the grouping and return one corpus-wide answer.}

\item{text}{The text column to scan, supplied unquoted. Any atomic column
is accepted and read as character, so a \code{factor} works and a numeric,
\code{Date} or logical one simply contains no emoji. A list column -- or a
data-frame column -- is refused rather than coerced, because coercing one
deparses it and the emoji found would be in the code rather than in your
data. What counts as an emoji is the same in every verb; see the
\emph{Detection} section of \link{tidyEmoji} for the one case that surprises
people, code points that are emoji only when they carry \code{U+FE0F}.}

\item{text_score}{Unquoted numeric column holding the text's own sentiment.}

\item{method}{\code{"difference"} (default) for the continuous gap, or
\code{"sign_flip"} for the categorical polarity-flip feature.}

\item{scale}{How to make the two scores comparable: \code{"rank"}, \code{"zscore"} or
\code{"none"}. Required -- there is no sensible default. \code{"rank"} and
\code{"zscore"} are computed over the rows carrying both an emoji score and a
\code{text_score}, not over the whole corpus, so rows with no scorable emoji
cannot shift the answer for the rows that have one.}

\item{where}{\code{"all"} (default) scores every emoji in the row; \code{"final"}
scores only the trailing run of emoji that ends the text.}

\item{threshold}{For \code{method = "difference"}, the absolute gap at or above
which \code{.emoji_incongruent} is \code{TRUE}. Default \code{1}, a full polarity swing on
the rank scale.}
}
\value{
\code{data}, as a tibble, with added columns \code{.emoji_n},
\code{.emoji_n_scored}, \code{.emoji_sentiment}, \code{.emoji_incongruity},
\code{.emoji_polarity_flip} and \code{.emoji_incongruent}.

\code{.emoji_n_scored} distinguishes the two ways the answer can be missing,
as it does in \code{\link[=emoji_sentiment]{emoji_sentiment()}}: \code{0} means the row had emoji that the
lexicon could not score, \code{NA} that it had no emoji to score. The four
derived columns are \code{NA} in both cases, and also wherever \code{text_score}
itself is missing or not finite.
}
\description{
\code{emoji_incongruity()} measures the signed gap between the sentiment a row's
emoji carry and the sentiment of its text. It is the sarcasm-detection
feature the NLP literature keeps rediscovering, and the mismatch variable
the marketing literature calls (in)congruence.
}
\details{
\code{.emoji_incongruity} is \code{emoji - text} after scaling, so it is positive when
the emoji is the more positive of the two. \code{"sign_flip"} is the categorical
version most sarcasm papers use and is computed on the \emph{unscaled} scores,
where the sign means something.

A row with no scorable emoji gets \code{NA}, never \code{0}: a neutral emoji and no
emoji at all are different states, and collapsing them silently biases every
downstream model. The same applies to a missing \code{text_score}, and to an
infinite one -- a scorer that overflows is reported and treated as missing
rather than left to turn every other row's z-score into \code{Inf}.

With \code{where = "final"} only the run of emoji that ends the text is scored:
both the illocutionary-force account of emoji and the P600 evidence on
ironic emoji are specifically about sentence-final glyphs. A text whose
emoji sit mid-sentence then has nothing eligible to score, so it gets \code{NA}
and \code{.emoji_n_scored = NA}, while \code{.emoji_n} still counts every emoji in the
row.

"Ends the text" is literal: only whitespace may follow the last glyph, so
\code{"great \\U0001f602"} has a final run and \code{"great \\U0001f602."} does not --
a trailing full stop, bracket or quote mark disqualifies it. The run itself
extends back over any glyphs separated from each other by whitespace alone,
so \code{"great \\U0001f602 \\U0001f60d"} contributes both. If your corpus
punctuates after emoji, strip trailing punctuation before scoring, or use
\code{where = "all"}.
}
\section{You supply the text score}{

tidyEmoji deliberately does not score text. \code{text_score} is a column you
produce with \pkg{tidytext} and AFINN or Bing, \pkg{sentimentr},
\pkg{vader}, or a transformer -- which keeps the method choice visible in
your script instead of buried in this package, and keeps our dependency
footprint where it is.

Because those methods live on wildly different scales (AFINN runs -5 to 5,
VADER -1 to 1, a model's logits on nothing in particular), \code{scale} has no
default: you have to say how the two sides were made comparable.
\code{"rank"} maps both to percentiles on \verb{[-1, 1]} and is the safest choice for
cross-method comparison; \code{"zscore"} standardises both; \code{"none"} compares the
raw numbers, which is only meaningful if your text score already lives on
the emoji lexicon's -1 to 1 scale.

\code{"rank"} and \code{"zscore"} are computed over the rows the comparison is defined
on -- those carrying both an emoji score and a \code{text_score} -- not over the
whole corpus. A percentile only means something relative to a population,
and the population the gap lives in is the scored subset, so rows with no
scorable emoji cannot move the answer for the rows that have one. Subsetting
the data to the scored rows before calling therefore gives the same numbers
as calling on everything.
}

\examples{
df <- data.frame(
  text = c("this is wonderful \U0001f621", "awful \U0001f621", "great \U0001f600"),
  score = c(0.9, -0.8, 0.7)
)
emoji_incongruity(df, text, score, scale = "none")
emoji_incongruity(df, text, score, scale = "none", method = "sign_flip")
}
\references{
An emoji centric approach to sarcasm detection in online
discourse. \emph{Scientific Reports} (2025). The influence of emoji meaning
multipleness on perceived online review helpfulness. \emph{Journal of Business
Research} (2022).
}
\seealso{
\code{\link[=emoji_congruence]{emoji_congruence()}} for the same engine under the marketing
framing; \code{\link[=emoji_incongruity_profile]{emoji_incongruity_profile()}} for which glyphs go against the
grain; \code{\link[=emoji_sentiment]{emoji_sentiment()}} for the emoji side on its own.
}
