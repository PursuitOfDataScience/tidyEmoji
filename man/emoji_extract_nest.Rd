% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emoji-extraction.R
\name{emoji_extract_nest}
\alias{emoji_extract_nest}
\title{Add a list-column of the emoji found in each row}
\usage{
emoji_extract_nest(data, text)
}
\arguments{
\item{data}{A data frame or tibble containing a text column. Grouped data
frames are accepted. The verbs that work a row at a time (adding columns,
or keeping and expanding rows) carry the grouping through to their result,
as \code{\link[dplyr:mutate]{dplyr::mutate()}} and \code{\link[dplyr:filter]{dplyr::filter()}} do. The verbs that pool across
rows -- the counts, the co-occurrence edge lists, the time series -- warn
that they ignore the grouping and return one corpus-wide answer.}

\item{text}{The text column to scan, supplied unquoted. What counts as an
emoji is the same in every verb; see the \emph{Detection} section of
\link{tidyEmoji} for the one case that surprises people, code points that
are emoji only when they carry \code{U+FE0F}.}
}
\value{
\code{data} with an added list-column \code{.emoji_unicode}.
}
\description{
\code{emoji_extract_nest()} returns \code{data} unchanged except for an added
list-column, \code{.emoji_unicode}, holding the emoji found in each row. Detection
is grapheme-aware, so skin-tone modifiers and ZWJ sequences (for example
family emoji) are kept intact as a single emoji.
}
\examples{
df <- data.frame(text = c("hi \U0001f600\U0001f603", "none"))
emoji_extract_nest(df, text)
}
\seealso{
\code{\link[=emoji_extract_unnest]{emoji_extract_unnest()}} for a long, counted form and
\code{\link[=emoji_tokens]{emoji_tokens()}} for one row per emoji with metadata.
}
