% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emoji-context.R
\name{emoji_context}
\alias{emoji_context}
\title{The text around each emoji occurrence}
\usage{
emoji_context(
  data,
  text,
  window = 5,
  unit = c("word", "char"),
  keep_text = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble containing a text column. Grouped data
frames are accepted. The verbs that work a row at a time (adding columns,
or keeping and expanding rows) carry the grouping through to their result,
as \code{\link[dplyr:mutate]{dplyr::mutate()}} and \code{\link[dplyr:filter]{dplyr::filter()}} do. The verbs that pool across
rows -- the counts, the co-occurrence edge lists, the time series -- warn
that they ignore the grouping and return one corpus-wide answer.}

\item{text}{The text column to scan, supplied unquoted. What counts as an
emoji is the same in every verb; see the \emph{Detection} section of
\link{tidyEmoji} for the one case that surprises people, code points that
are emoji only when they carry \code{U+FE0F}.}

\item{window}{Size of the context window on each side, in tokens
(\code{unit = "word"}) or characters (\code{unit = "char"}). Default \code{5}.}

\item{unit}{\code{"word"} (default) or \code{"char"}.}

\item{keep_text}{If \code{TRUE}, also return the row's original text column.
Default \code{FALSE}.}
}
\value{
A tibble with one row per emoji occurrence, in reading order, and
columns \code{.row_number} (position of the entry in \code{data}), \code{.position} (the
character position at which the emoji starts), \code{.emoji},
\code{.emoji_context_left}, \code{.emoji_context_right} and \code{.emoji_context} (the two
sides joined by a space -- the co-text without the glyph). Rows with no
emoji contribute nothing.
}
\description{
\code{emoji_context()} returns one row per emoji occurrence with a window of the
text on either side of it. It is the primitive the context-dependent
analyses need: emoji are polysemous, and what a glyph means in a message is
decided by its co-text, not by a lexicon.
}
\details{
Windows are taken from the text with \emph{all} emoji blanked out, so a
neighbouring emoji never lands in a context window and character offsets stay
exact. With \code{unit = "word"} a token is a maximal run of non-whitespace
characters, the same definition \code{\link[=emoji_density]{emoji_density()}} uses; with \code{unit = "char"}
the window is a literal character count after trimming the whitespace next
to the emoji.

Tokenisation stops there on purpose. If you need stemming, stopword removal
or sentence splitting, pass the result to \pkg{tokenizers} or \pkg{tidytext}
rather than expecting this verb to grow a tokeniser.
}
\examples{
df <- data.frame(text = c("the coffee was cold \U0001f622 again",
                          "no emoji here"))
emoji_context(df, text, window = 2)
emoji_context(df, text, window = 6, unit = "char")
}
\seealso{
\code{\link[=emoji_collocations]{emoji_collocations()}} for the corpus-level view;
\code{\link[=emoji_position]{emoji_position()}} for where emoji sit in a text.
}
