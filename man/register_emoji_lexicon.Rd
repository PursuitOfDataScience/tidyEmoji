% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emoji-lexicon-api.R
\name{register_emoji_lexicon}
\alias{register_emoji_lexicon}
\title{Register a custom emoji lexicon}
\usage{
register_emoji_lexicon(name, tbl, by = "emoji")
}
\arguments{
\item{name}{Name to register the lexicon under.}

\item{tbl}{A data frame. Must contain a glyph column named \code{by} (default
\code{"emoji"}) and at least one score column, and every score column present
must be numeric or logical -- a text column is rejected here rather than
returning \code{NA} for every score at first use. See \code{\link[=emoji_score]{emoji_score()}} for the
one-row-per-emoji requirement, which is checked when the lexicon is used.}

\item{by}{Name of the column holding the emoji glyph, as a single string.
Default \code{"emoji"}.}
}
\value{
Invisibly, the registered lexicon, with a \code{key} column holding the
code-point key of each glyph. A \code{key} column already in \code{tbl} is
replaced rather than trusted: the registry looks a lexicon up by that
column, so one holding anything else would resolve every row to nothing.
}
\description{
\code{register_emoji_lexicon()} adds a user-supplied lexicon to the in-session
registry so it can be referenced by name in \code{\link[=emoji_score]{emoji_score()}},
\code{\link[=emoji_sentiment]{emoji_sentiment()}} or \code{\link[=emoji_emotion]{emoji_emotion()}}. The lexicon is normalised through
the package's codepoint key (\code{U+FE0F} stripped), so a lexicon keyed on
unqualified glyphs still matches qualified text.
}
\details{
Registration lasts for the session; it is not written to disk. Registering
a name that is already taken replaces the table under it, without warning
and without a way to get the old one back: there is no public counterpart
that removes a registration, so re-registering is how a lexicon is
changed. \code{\link[=emoji_lexicons]{emoji_lexicons()}} shows what is currently registered.
}
\examples{
my_lex <- data.frame(
  emoji = c("\U0001f600", "\U0001f621"),
  score = c(0.9, -0.8)
)
register_emoji_lexicon("mine", my_lex)
emoji_lexicons()
emoji_score(data.frame(text = "great \U0001f600"), text, lexicon = "mine")
}
\seealso{
\code{\link[=emoji_lexicons]{emoji_lexicons()}} to list lexicons; \code{\link[=emoji_score]{emoji_score()}} to use one.
}
