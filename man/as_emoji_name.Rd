% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emoji-translate.R
\name{as_emoji_name}
\alias{as_emoji_name}
\alias{as_emoji_shortcode}
\alias{as_emoji}
\title{Vector helpers: convert emoji to/from names and shortcodes}
\usage{
as_emoji_name(x)

as_emoji_shortcode(x)

as_emoji(x)
}
\arguments{
\item{x}{A character vector of emoji glyphs (for \code{as_emoji_name},
\code{as_emoji_shortcode}) or of shortcodes/names (for \code{as_emoji}).}
}
\value{
A character vector the same length as \code{x}.
}
\description{
Small vector-level helpers for ad-hoc use. They do not take a data frame.
}
\details{
\itemize{
\item \code{as_emoji_name(x)} maps emoji glyphs to their Unicode names.
\item \code{as_emoji_shortcode(x)} maps emoji glyphs to a shortcode.
\item \code{as_emoji(x)} maps names/shortcodes to the emoji glyph (emojize).
}

All three resolve through \code{emoji_key()}, so qualified emoji (carrying
\code{U+FE0F}) and unqualified forms resolve identically. Unmatched inputs return
\code{NA}.

\code{as_emoji()} accepts either namespace in the same argument, and 464 strings
belong to both -- they are the exact Unicode name of one emoji and a
shortcode alias of another. It resolves them in a fixed order: \strong{exact
Unicode name first}, then shortcode, then \pkg{emoji}'s own name table. An
exact name match is the stronger signal, so \code{as_emoji("dog")} is the emoji
actually \emph{named} "dog" (a dog, \verb{U+1F415}), not the one whose alias is
\verb{:dog:} (a dog face, \verb{U+1F436}).

For 17 of those 464 strings the two namespaces disagree, and there
\code{as_emoji()} and \code{\link[=text_to_emoji]{text_to_emoji()}} differ \emph{by design}: a \verb{:dog:} token is
explicitly delimited as a shortcode, so \code{\link[=text_to_emoji]{text_to_emoji()}} reads it in the
shortcode namespace and produces the dog face. The pattern is an emoji whose
name is a bare noun versus the "... face" variant that carries the alias
(\code{cat}, \code{cow}, \code{pig}, \code{tiger}, \code{mouse}, \code{rabbit}), or a plain object versus a
decorated one (\code{umbrella}, \code{snowman}, \code{calendar}, \code{sunglasses}). Pass a
shortcode through \code{\link[=text_to_emoji]{text_to_emoji()}}, or the full Unicode name (\code{"dog face"})
to \code{as_emoji()}, if you need one namespace specifically.
}
\section{Which shortcode you get}{

\code{as_emoji_shortcode()} returns one shortcode \emph{per emoji}, not per spelling:
the first alias of the emoji's fully-qualified (RGI) form. That is what
makes it agree with \code{\link[=emoji_to_text]{emoji_to_text()}} and survive a round trip, but it is
not always the first alias of the glyph you passed in. 344 codepoint keys
have a different first alias on each of their two spellings, so for 175 of
the catalogue's 5042 rows the two answers differ:
\code{as_emoji_shortcode("\\u2764")} is \code{"heart"}, the qualified heart's alias,
where the bare \code{U+2764} row's own first alias is \code{"red_heart"}.

\code{\link[=emoji_search]{emoji_search()}} reports the other one -- the matched row's own alias, since
a search result is a row -- so the two verbs can disagree on the same glyph.
Both resolve back to the same emoji through \code{\link[=text_to_emoji]{text_to_emoji()}}, which reads
every alias, so where both answer the disagreement is cosmetic. Where they
differ in substance is the 189 rows that have no alias: \code{\link[=emoji_search]{emoji_search()}}
reports \code{NA} for those, while \code{as_emoji_shortcode()} still answers for all
189, having borrowed the alias of the glyph's other spelling.
}

\examples{
as_emoji_name(c("\U0001f600", "\u2764\ufe0f"))
as_emoji_shortcode(c("\U0001f600", "\u2764\ufe0f"))
as_emoji(c("grinning", "heart"))
}
\seealso{
\code{\link[=emoji_to_text]{emoji_to_text()}}, \code{\link[=text_to_emoji]{text_to_emoji()}} for the data-frame verbs.
}
