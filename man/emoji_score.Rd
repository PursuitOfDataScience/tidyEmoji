% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emoji-lexicon-api.R
\name{emoji_score}
\alias{emoji_score}
\title{Score emoji in a text column against any lexicon}
\usage{
emoji_score(data, text, lexicon, by = "emoji", score = NULL)
}
\arguments{
\item{data}{A data frame or tibble containing a text column.}

\item{text}{The text column to scan, supplied unquoted.}

\item{lexicon}{Either a string naming a bundled or registered lexicon, or a
data frame. For data frames, \code{by} names the glyph column and \code{score} the
score column.}

\item{by}{Glyph column name when \code{lexicon} is a data frame. Default
\code{"emoji"}.}

\item{score}{Score column name when \code{lexicon} is a data frame. If \code{NULL},
\code{"sentiment_score"} then \code{"score"} are tried.}
}
\value{
\code{data}, as a tibble, with \code{.emoji_score} (per-row mean),
\code{.emoji_n_scored} (emoji found in the lexicon) and \code{.emoji_n} (total emoji)
added.
}
\description{
\code{emoji_score()} is the generic scorer that the friendly verbs
(\code{\link[=emoji_sentiment]{emoji_sentiment()}}, \code{\link[=emoji_emotion]{emoji_emotion()}}) sit on top of. It joins each row's
emoji to \code{lexicon} through \code{emoji_key()} and returns the per-row mean of the
\code{score} column, plus the number of emoji scored. Bring your own lexicon, or
name a bundled / registered one.
}
\examples{
df <- data.frame(text = c("love \U0001f60d", "angry \U0001f621", "meh"))
emoji_score(df, text, lexicon = "novak2015")

# a bring-your-own lexicon
own <- data.frame(emoji = c("\U0001f600", "\U0001f621"),
                  score = c(0.9, -0.8))
emoji_score(df, text, lexicon = own)
}
\seealso{
\code{\link[=emoji_lexicons]{emoji_lexicons()}}, \code{\link[=register_emoji_lexicon]{register_emoji_lexicon()}}.
}
