% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emoji-ambiguity.R
\name{emoji_ambiguity}
\alias{emoji_ambiguity}
\title{How ambiguous is each emoji?}
\usage{
emoji_ambiguity(x = NULL, measure = "entropy")
}
\arguments{
\item{x}{Optional character vector of emoji glyphs to report on. The default,
\code{NULL}, returns every emoji in the lexicon, most ambiguous first. Glyphs
absent from the lexicon come back with \code{NA} statistics.}

\item{measure}{Which ambiguity statistic to put in the \code{ambiguity} column:
one of \code{"entropy"} (default), \code{"gini"}, \code{"neutral_share"} or \code{"ci_width"}.}
}
\value{
A tibble with columns \code{emoji}, \code{key} (the codepoint-normalised join
key), \code{n_annotations}, \code{p_neg}, \code{p_neu}, \code{p_pos}, \code{ambiguity} and \code{rank}.
\code{rank} is \code{1} for the most ambiguous emoji; glyphs with identical
\code{ambiguity} \strong{share} the lowest rank of their group and the next distinct
value skips ahead accordingly (\code{rank()}'s \code{ties.method = "min"}), so ranks
are not necessarily consecutive. With \code{x = NULL} rows are ordered by
\code{rank} with ties broken by the glyph, so the order is deterministic; with
\code{x} supplied the result has one row per element of \code{x}, in the same
order.
}
\description{
\code{emoji_ambiguity()} reports, for every emoji in the Emoji Sentiment Ranking
(see \link{emoji_sentiment_lexicon}), how much its human annotators disagreed
about whether it was negative, neutral or positive. Miller et al. (2016)
found that readers of the same rendering disagree about a quarter of the
time; the bundled lexicon keeps the raw annotation counts behind its
collapsed score, so that disagreement can be reported as a number rather
than described as a caveat.
}
\details{
The four measures are computed from the annotation shares
\verb{(p_neg, p_neu, p_pos)}:
\itemize{
\item \code{"entropy"} (the default) is Shannon entropy in nats: 0 when the
annotators were unanimous, \code{log(3)} (about 1.0986) when they split evenly
three ways.
\item \code{"gini"} is the Gini impurity, \code{1 - sum(p^2)}: 0 when unanimous, 2/3 at
maximum disagreement.
\item \code{"neutral_share"} is \code{p_neu} on its own, for the "is this emoji simply
uninformative?" question.
\item \code{"ci_width"} is the width of a 95\% Wald interval around the glyph's
sentiment score. Unlike the other three it shrinks as the number of
annotations grows, so it answers "how well do we know this score?" rather
than "how much do readers disagree?".
}

\code{rank} is always computed over the whole lexicon (1 = most ambiguous), so a
rank keeps its meaning when \code{x} selects a handful of glyphs.

\strong{Read \code{n_annotations} before you read the ranking.} The lexicon's
annotation counts are wildly uneven -- the median glyph has 18, and 69\% have
fewer than 50 -- and the first three measures are shape statistics that do
not care how many annotations produced the shape. A glyph seen by three
annotators who split one-one-one scores the maximum entropy of \code{log(3)} on
that evidence alone, which is why five of the rows tied at \code{rank = 1} have
3, 3, 3, 9 and 15 annotations, and why 11 of the top 20 have fewer than 50.

Three of those five are not emoji at all. The lexicon was built from 2015
tweets and 233 of its 969 rows are characters absent from the reference
table -- box-drawing characters, dingbats, enclosed letters -- so the head
of the ranking can show a glyph such as \verb{U+250C} that no corpus this
package analyses will ever yield. They carry 6\% of the lexicon's
annotations and 86\% of them have fewer than 50, so the \code{n_annotations}
filter recommended here removes 200 of the 233 as a side effect. See the
\emph{Detection limitations} section of \link{emoji_sentiment_lexicon} for what the
rest of them are.

The bias is at the \emph{top} of the ranking specifically, not across it: a
thinly annotated glyph is usually unanimous, so entropy is positively
correlated with the annotation count overall (Spearman 0.56). What three
annotators can do that thousands cannot is hit the exact maximum. So filter
on \code{n_annotations} before interpreting the head of the table, as the
introduction vignette does. That advice applies to \code{"ci_width"} too, and
the reason is worth stating plainly, because the obvious reading of a
confidence width is that it has already accounted for thin evidence:

\code{"ci_width"} is a \strong{Wald} interval, so it scales as \code{1 / sqrt(n)} only \emph{at
a given spread}, and it is exactly zero wherever the spread is zero. For a
glyph whose annotators were unanimous the estimated variance is 0 whatever
\code{n} is, so the interval has zero width on one annotation just as on eight
thousand. That is the textbook degeneracy of the Wald interval at a
boundary proportion, not a property of the data: \strong{166 of the lexicon's 969
rows report \code{ci_width = 0}, and their annotation counts run from 1 to 68.}
So \code{ci_width} does not rescue a thin glyph -- ranked ascending it puts the
thinnest unanimous ones first, as the most certain rows in the table.
\code{n_annotations} remains the column to filter on; \code{ci_width} separates
well-known from poorly-known scores only among glyphs that are not
unanimous.
}
\examples{
head(emoji_ambiguity())

# the head of that table is glyphs a handful of annotators disagreed about;
# filter on n_annotations before reading it as a finding
amb <- emoji_ambiguity()
head(amb[amb$n_annotations >= 500, ])

emoji_ambiguity(c("\U0001f602", "\U0001f643"))
head(emoji_ambiguity(measure = "ci_width"))
}
\references{
Miller H, Thebault-Spieker J, Chang S, Johnson I, Terveen L,
Hecht B (2016). "Blissfully Happy" or "Ready to Fight": Varying
Interpretations of Emoji. \emph{ICWSM 2016}.
}
\seealso{
\code{\link[=emoji_risk]{emoji_risk()}} for the per-row version, \code{\link[=emoji_flag_ambiguous]{emoji_flag_ambiguous()}}
for the emoji in your own corpus, and \code{\link[=emoji_sentiment]{emoji_sentiment()}} with
\code{se = TRUE} for the uncertainty around a score.
}
