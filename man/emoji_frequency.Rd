% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/top-n-emojis.R
\name{emoji_frequency}
\alias{emoji_frequency}
\title{Frequency of every emoji in a text column}
\usage{
emoji_frequency(data, text)
}
\arguments{
\item{data}{A data frame or tibble containing a text column. Grouped data
frames are accepted. The verbs that work a row at a time (adding columns,
or keeping and expanding rows) carry the grouping through to their result,
as \code{\link[dplyr:mutate]{dplyr::mutate()}} and \code{\link[dplyr:filter]{dplyr::filter()}} do. The verbs that pool across
rows -- the counts, the co-occurrence edge lists, the time series -- warn
that they ignore the grouping and return one corpus-wide answer.}

\item{text}{The text column to scan, supplied unquoted. What counts as an
emoji is the same in every verb; see the \emph{Detection} section of
\link{tidyEmoji} for the one case that surprises people, code points that
are emoji only when they carry \code{U+FE0F}.}
}
\value{
A tibble with columns \code{emoji}, \code{name}, \code{shortcode}, \code{group} and \code{n},
sorted by descending \code{n} with ties broken by the glyph so the order is
deterministic.
}
\description{
\code{emoji_frequency()} counts how often each emoji appears across the whole text
column (an entry containing the same emoji twice contributes 2) and returns a
tibble sorted by descending count, with each emoji's name, shortcode and
category.
}
\examples{
df <- data.frame(text = c("\U0001f600\U0001f600", "\U0001f621"))
emoji_frequency(df, text)
}
\seealso{
\code{\link[=top_n_emojis]{top_n_emojis()}} for just the most frequent emoji.
}
