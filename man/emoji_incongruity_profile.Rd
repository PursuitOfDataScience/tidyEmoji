% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emoji-incongruity.R
\name{emoji_incongruity_profile}
\alias{emoji_incongruity_profile}
\title{Which emoji go against the grain of their text?}
\usage{
emoji_incongruity_profile(
  data,
  text,
  text_score,
  method = c("difference", "sign_flip"),
  scale,
  where = c("all", "final"),
  threshold = 1,
  min_n = 5
)
}
\arguments{
\item{data}{A data frame or tibble containing a text column.}

\item{text}{The text column to scan, supplied unquoted.}

\item{text_score}{Unquoted numeric column holding the text's own sentiment.}

\item{method}{\code{"difference"} (default) for the continuous gap, or
\code{"sign_flip"} for the categorical polarity-flip feature.}

\item{scale}{How to make the two scores comparable: \code{"rank"}, \code{"zscore"} or
\code{"none"}. Required -- there is no sensible default.}

\item{where}{\code{"all"} (default) scores every emoji in the row; \code{"final"}
scores only the trailing run of emoji that ends the text.}

\item{threshold}{For \code{method = "difference"}, the absolute gap at or above
which \code{.emoji_incongruent} is \code{TRUE}. Default \code{1}, a full polarity swing on
the rank scale.}

\item{min_n}{Minimum number of scored occurrences for an emoji to be
reported. Default \code{5}.}
}
\value{
A tibble with one row per emoji: \code{emoji}, \code{name}, \code{n} (scored
occurrences), \code{mean_incongruity}, \code{sd_incongruity}, \code{n_flips} and
\code{flip_rate}, sorted by descending \code{flip_rate}.
}
\description{
\code{emoji_incongruity_profile()} aggregates \code{\link[=emoji_incongruity]{emoji_incongruity()}} by glyph: for
each emoji, how far from its host text's sentiment it typically sits, and how
often it appears with the opposite polarity. Those are the candidate irony
markers in your corpus.
}
\details{
Incongruity is a property of a row, so every emoji in a row is credited with
that row's gap. A glyph that habitually shares a message with a genuinely
incongruent one will therefore inherit some of its score; read \code{n} alongside
\code{flip_rate} before drawing conclusions from a handful of occurrences.
}
\examples{
df <- data.frame(
  text = c("great \U0001f621", "lovely \U0001f621", "awful \U0001f621"),
  score = c(0.8, 0.7, -0.9)
)
emoji_incongruity_profile(df, text, score, scale = "none", min_n = 1)
}
\seealso{
\code{\link[=emoji_incongruity]{emoji_incongruity()}}.
}
