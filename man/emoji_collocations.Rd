% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emoji-context.R
\name{emoji_collocations}
\alias{emoji_collocations}
\title{Which words keep company with which emoji}
\usage{
emoji_collocations(
  data,
  text,
  window = 5,
  min_n = 3,
  measure = c("pmi", "count")
)
}
\arguments{
\item{data}{A data frame or tibble containing a text column. Grouped data
frames are accepted. The verbs that work a row at a time (adding columns,
or keeping and expanding rows) carry the grouping through to their result,
as \code{\link[dplyr:mutate]{dplyr::mutate()}} and \code{\link[dplyr:filter]{dplyr::filter()}} do. The verbs that pool across
rows -- the counts, the co-occurrence edge lists, the time series -- warn
that they ignore the grouping and return one corpus-wide answer.}

\item{text}{The text column to scan, supplied unquoted. What counts as an
emoji is the same in every verb; see the \emph{Detection} section of
\link{tidyEmoji} for the one case that surprises people, code points that
are emoji only when they carry \code{U+FE0F}.}

\item{window}{Context window on each side, in words. Default \code{5}.}

\item{min_n}{Minimum number of co-occurrences for a pair to be reported.
Default \code{3}.}

\item{measure}{Sort order: \code{"pmi"} (default) or \code{"count"}. Both columns are
always returned.}
}
\value{
A tibble with columns \code{emoji}, \code{word}, \code{n} (co-occurrences) and
\code{pmi}, shaped like \code{widyr::pairwise_count()} output so it drops into
existing tidytext workflows.
}
\description{
\code{emoji_collocations()} counts the words that appear near each emoji across a
corpus and scores the association with pointwise mutual information. It is
the corpus-derived alternative to importing a fixed sense inventory: the
senses come from \emph{your} texts, so they cannot be stale and carry no licence
baggage.
}
\details{
Each emoji occurrence contributes its context window (see
\code{\link[=emoji_context]{emoji_context()}}). A word is counted once per occurrence however often it
repeats inside that window. Words are lower-cased and stripped of leading and
trailing punctuation; no stopword list is applied, because which stopwords
are right is a decision for your analysis, not for this package -- filter the
result with \pkg{tidytext}'s \code{stop_words} if you want one.

PMI is \code{log(n(e, w) * N / (n(e) * n(w)))}, with \code{N} the total number of
emoji-word co-occurrence events. Marginals are computed over \emph{all}
co-occurrences before \code{min_n} filters the rows, so a rare pairing is scored
against the full corpus rather than against the surviving subset.

Glyphs are canonicalised through the package's codepoint key, so qualified
and unqualified forms of the same emoji share one row.
}
\examples{
df <- data.frame(text = c("cold coffee \U0001f622",
                          "coffee again \U0001f622",
                          "warm tea \U0001f60a"))
emoji_collocations(df, text, min_n = 1)
}
\seealso{
\code{\link[=emoji_context]{emoji_context()}} for the occurrence-level windows this aggregates.
}
