% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emoji-time.R
\name{emoji_adoption_lag}
\alias{emoji_adoption_lag}
\title{How long did this population take to adopt each emoji?}
\usage{
emoji_adoption_lag(data, text, time)
}
\arguments{
\item{data}{A data frame or tibble containing a text column. Grouped data
frames are accepted. The verbs that work a row at a time (adding columns,
or keeping and expanding rows) carry the grouping through to their result,
as \code{\link[dplyr:mutate]{dplyr::mutate()}} and \code{\link[dplyr:filter]{dplyr::filter()}} do. The verbs that pool across
rows -- the counts, the co-occurrence edge lists, the time series -- warn
that they ignore the grouping and return one corpus-wide answer.}

\item{text}{The text column to scan, supplied unquoted. What counts as an
emoji is the same in every verb; see the \emph{Detection} section of
\link{tidyEmoji} for the one case that surprises people, code points that
are emoji only when they carry \code{U+FE0F}.}

\item{time}{Unquoted column of dates or date-times (\code{Date}, \code{POSIXct}, or
character in \code{"YYYY-MM-DD"} form). A date-time is bucketed by the calendar
day it \emph{displays} as in its own timezone, not by its UTC day: an emoji
posted at 23:30 New York time belongs to that day, not to the next one.
Character values that cannot be read as a date warn and are dropped.}
}
\value{
A tibble with one row per emoji, most frequent first: \code{emoji},
\code{name}, \code{n}, \code{version}, \code{release_date}, \code{first_seen} and \code{lag_days}.
\code{lag_days} is \code{NA} when the release date of the version is unknown.
}
\description{
\code{emoji_adoption_lag()} compares the date an emoji was first used in your
corpus with the date Unicode released it, giving a per-glyph adoption lag in
days.
}
\details{
A lag is only as good as the corpus window: an emoji released before your
data begins will look adopted on day one, so read the lag together with \code{n}
and the span of your data. Negative lags mean the corpus contains a glyph
before its official release date -- usually a vendor shipping early, or a
timestamp problem worth investigating.

Occurrences whose time is missing or unparseable are dropped.
}
\examples{
df <- data.frame(
  when = as.Date(c("2021-01-01", "2022-06-01")),
  text = c("\U0001f600", "\U0001f97a")
)
emoji_adoption_lag(df, text, when)
}
\seealso{
\code{\link[=emoji_version_profile]{emoji_version_profile()}}, \code{\link[=emoji_unicode_releases]{emoji_unicode_releases()}}.
}
