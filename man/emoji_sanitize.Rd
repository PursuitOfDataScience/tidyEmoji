% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emoji-sanitize.R
\name{emoji_sanitize}
\alias{emoji_sanitize}
\title{Apply an explicit emoji policy to a text column}
\usage{
emoji_sanitize(
  data,
  text,
  policy = "keep",
  placeholder = "[emoji]",
  wrap = ":{x}:"
)
}
\arguments{
\item{data}{A data frame or tibble containing a text column. Grouped data
frames are accepted. The verbs that work a row at a time (adding columns,
or keeping and expanding rows) carry the grouping through to their result,
as \code{\link[dplyr:mutate]{dplyr::mutate()}} and \code{\link[dplyr:filter]{dplyr::filter()}} do. The verbs that pool across
rows -- the counts, the co-occurrence edge lists, the time series -- warn
that they ignore the grouping and return one corpus-wide answer.}

\item{text}{The text column to scan, supplied unquoted. Any atomic column
is accepted and read as character, so a \code{factor} works and a numeric,
\code{Date} or logical one simply contains no emoji. A list column -- or a
data-frame column -- is refused rather than coerced, because coercing one
deparses it and the emoji found would be in the code rather than in your
data. What counts as an emoji is the same in every verb; see the
\emph{Detection} section of \link{tidyEmoji} for the one case that surprises
people, code points that are emoji only when they carry \code{U+FE0F}.}

\item{policy}{One of \code{"keep"} (default), \code{"strip"}, \code{"name"},
\code{"shortcode"} or \code{"placeholder"}.}

\item{placeholder}{Replacement token for \code{policy = "placeholder"}. Default
\code{"[emoji]"}. Ignored, and not even validated, under the other four
policies.}

\item{wrap}{Template for \code{policy = "shortcode"}, with \code{{x}} standing for the
shortcode. Default \code{":{x}:"}. Ignored under the other four policies, as
it is in \code{\link[=emoji_to_text]{emoji_to_text()}} for \code{format = "name"}.}
}
\value{
\code{data}, as a tibble, with the text column rewritten in place (same
column name). \code{NA} entries stay \code{NA}.
}
\description{
\code{emoji_sanitize()} rewrites a text column under one named policy: keep the
emoji, delete them, spell them out as names or shortcodes, or replace them
with a placeholder token. The value is not new capability -- most of it
exists across \code{\link[=emoji_to_text]{emoji_to_text()}} and the extraction verbs -- but a single
argument that says which choice was made, so that "we replaced emoji with
their Unicode names" becomes a reproducibility statement rather than a
forgotten line of \code{gsub()}.
}
\details{
The policies:
\itemize{
\item \code{"keep"} returns the text untouched. It is the honest baseline for an A/B
comparison, and it means the policy argument can stay in the script even
when the answer is "do nothing".
\item \code{"strip"} deletes the emoji. Because deleting a glyph can leave two spaces
where there was one, \code{strip} also collapses runs of spaces and tabs and
trims whitespace off both ends, using Unicode's \code{White_Space} set as the
rest of the package does, so a no-break or ideographic space left at an
end goes too. It is the only policy that touches anything but the emoji.
Removing a span makes its two neighbours adjacent, and on malformed
input those two can spell an emoji the original text did not contain (a
bare \code{U+2603} beside an orphan \code{U+FE0F} becomes the qualified snowman),
so \code{strip} repeats until there is nothing left to remove. It is the one
policy whose result is emoji-free whatever you hand it.
\item \code{"name"} and \code{"shortcode"} substitute the Unicode name
("grinning face") or the GitHub-style alias (":grinning:"), exactly as
\code{\link[=emoji_to_text]{emoji_to_text()}} does -- including its rule that a glyph with no known
name or alias is \strong{left in place unchanged}. A ZWJ sequence too new for
the installed catalogue is detected but cannot be named, so these two
policies can return a column that still holds emoji. \code{"strip"} never
does. Neither does \code{"placeholder"}, unless the token you supply is
itself emoji-forming: one that \emph{is} an emoji survives by definition, and
a lone combining character such as \code{U+FE0F} binds to whatever the
removed glyph was standing next to. \code{name} is also the accessibility
answer: it is what a screen reader announces.
\item \code{"placeholder"} substitutes a fixed token, which keeps the \emph{position} of
an emoji as a feature while removing its identity. An empty
\code{placeholder} is a deletion rather than a substitution, so it gets
\code{"strip"}'s repeat pass (but not its whitespace tidying).
}

Replacements go exactly where the glyph was, with no padding, so a grinning
face glued to the end of a word yields \code{"wordgrinning face"}. If your
tokeniser needs whitespace around them, use \code{"placeholder"} with a padded
placeholder such as \code{" [emoji] "}.

\code{placeholder} and \code{wrap} belong to one policy each and are ignored by the
rest, silently and without being validated. That is deliberate rather than
an oversight: the point of this verb is that \code{policy} can be a variable,
so a script sweeping all five with one \verb{placeholder =} set would otherwise
be warned at four fifths of its calls. Nothing is lost either way, since
an ignored argument cannot change the answer. Contrast
\code{\link[=emoji_incongruity]{emoji_incongruity()}}'s \code{threshold}, which \emph{does} warn when
\code{method = "sign_flip"} makes it inert: that one is a number the caller
chose in order to change a result, not a companion belonging to a branch.
}
\section{Which policies can be undone}{

The five policies are not five parallel options: they are a ladder of
information loss, and how far down it you step is invisible until you try to
put the emoji back after the model call.\tabular{llll}{
   \code{policy} \tab \code{"great <U+1F600> work"} becomes \tab Restorable with \code{\link[=text_to_emoji]{text_to_emoji()}}? \tab What is lost \cr
   \code{"keep"} \tab \verb{great <U+1F600> work} \tab yes \tab nothing \cr
   \code{"shortcode"} \tab \code{great :grinning: work} \tab \strong{yes} \tab nothing \cr
   \code{"name"} \tab \verb{great grinning face work} \tab no \tab the delimiters; the name is now ordinary words \cr
   \code{"placeholder"} \tab \verb{great [emoji] work} \tab no \tab \emph{which} emoji -- the position survives \cr
   \code{"strip"} \tab \verb{great work} \tab no \tab that there was an emoji at all \cr
}


So if the pipeline has to restore emoji downstream, \code{"shortcode"} is the
only policy that permits it, and it holds up on the awkward cases:
skin-tone modifiers, flags, ZWJ sequences and keycaps all come back.
Measured against the whole reference table of \pkg{emoji} 16.0.0: for all
3790 emoji in their canonical (fully qualified) spelling -- the spelling a
keyboard emits and text normally holds -- \strong{the round trip returns the
original text byte for byte, 100\% of the time}.

\strong{That row of the table assumes the default \code{wrap}.} Restoring the text
means \code{\link[=text_to_emoji]{text_to_emoji()}} can find the token, and it reads exactly
\verb{:shortcode:} -- a colon, the alias, a colon. So \code{wrap} is part of the
reversibility contract, not a cosmetic choice:
\itemize{
\item \code{wrap = ":{x}:"} (the default) restores the original text exactly.
\item A \code{wrap} that decorates the token -- \code{"[:{x}:]"}, \code{":{x}:!"} -- brings the
emoji back but leaves the decoration behind, so the emoji is restored and
the text is not.
\item A \code{wrap} with no \verb{:token:} at all -- \code{"{x}"}, \code{"<{x}>"}, \code{"@{x}@"},
\code{":{x}"} -- restores nothing: the shortcode stays in the text as an
ordinary word. This is the case to watch, because it fails silently.
\item \code{wrap = "::{x}::"} is worse than either: the inner \verb{:grinning:} matches,
so the emoji comes back wrapped in the leftover colons (\verb{:<U+1F600>:}) and
a further round trip keeps adding to them.
}

Change \code{wrap} for readability by all means, but not on a column you intend
to restore.

\strong{The default \code{wrap} also assumes something about the input text, and this is
the one failure mode that choosing it does not remove.} \code{\link[=text_to_emoji]{text_to_emoji()}}
cannot tell a \verb{:shortcode:} token this verb wrote from one the text already
held, so a corpus that arrives with literal shortcode tokens in it gains an
emoji where it had none. That is not a corner case: it is how people type in
the Slack, Discord and GitHub exports the LLM-preprocessing workflow is aimed
at.
\itemize{
\item \code{"nice work :thumbsup:"} sanitises unchanged, and restores as
\code{"nice work "} plus \verb{U+1F44D}. Emoji count 0 before the round trip, 1
after.
\item It compounds with real emoji rather than replacing them:
\code{"nice work :thumbsup:"} followed by \verb{U+1F600} restores with two emoji.
\item The damage is bounded at one pass. A second round trip changes nothing,
because by then the token is a glyph.
}

The colon hazards \code{\link[=text_to_emoji]{text_to_emoji()}} \emph{does} handle are the ones that are not
shortcode-shaped: a clock time (\code{"meet at 10:30"}) and a ratio
(\code{"ratio 3:4"}) both survive untouched, because the token it matches is a
colon, alias characters, and a closing colon. So the contract is precise:
the round trip restores the original text exactly when the only
\verb{:shortcode:}-shaped tokens in it are the ones this verb wrote. If your text
may hold real ones, compare \code{emoji_density()}'s \code{.emoji_n} before and after,
which is the cheapest way to see the inflation.

Unicode also lists shorter spellings of the same emoji, with the \code{U+FE0F}
presentation selectors omitted. Feed one of those in and the round trip
returns the \emph{canonical} spelling instead: \verb{U+270C} comes back as
\verb{U+270C U+FE0F}. Across all 4853 catalogued spellings that is 79.5\%
byte-identical, and the remaining 20.5\% differ by \code{U+FE0F} alone -- never
by more. The emoji is always the same emoji, and every tidyEmoji lookup
treats the two spellings as one, so this matters only if you are diffing raw
bytes on text that had its selectors stripped upstream.

\code{"placeholder"} keeps \emph{where} but not \emph{which}, which is enough to use "an
emoji was here" as a model feature and not enough to reconstruct the text.
\code{"name"} is the accessibility answer rather than the reversible one -- it
is what a screen reader announces.
}

\examples{
df <- data.frame(text = c("ship it \U0001f680", "no emoji"))
emoji_sanitize(df, text, policy = "strip")
emoji_sanitize(df, text, policy = "name")
emoji_sanitize(df, text, policy = "placeholder")
}
\seealso{
\code{vignette("reversible-preprocessing", package = "tidyEmoji")}
runs this ladder end to end, including the round trip on skin tones,
flags, keycaps and ZWJ sequences; \code{\link[=emoji_token_cost]{emoji_token_cost()}} for what the emoji
are costing you; \code{\link[=emoji_to_text]{emoji_to_text()}} for the name/shortcode rewrite on its
own.
}
