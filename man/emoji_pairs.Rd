% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emoji-relate.R
\name{emoji_pairs}
\alias{emoji_pairs}
\title{Co-occurring emoji pairs}
\usage{
emoji_pairs(data, text, doc_id = NULL, directed = FALSE, sort = TRUE)
}
\arguments{
\item{data}{A data frame or tibble containing a text column.}

\item{text}{The text column to scan, supplied unquoted.}

\item{doc_id}{Optional unquoted column identifying documents. Rows sharing
a value are treated as one document. Default: each row is a document.}

\item{directed}{If \code{TRUE}, pairs are ordered by first appearance: a
document where the tears-of-joy emoji appears before the heart-eyes emoji
counts towards (tears-of-joy, heart-eyes), not the reverse. Default
\code{FALSE} (unordered pairs, with \code{item1} sorted before \code{item2}).}

\item{sort}{If \code{TRUE} (default), sort by descending \code{n} (ties broken by
\code{item1}, \code{item2} so the order is deterministic).}
}
\value{
A tibble with columns \code{item1}, \code{item2} and \code{n}. Empty (but typed)
when no document contains two distinct emoji.
}
\description{
\code{emoji_pairs()} returns a tidy edge list of the emoji that appear together
in the same document: one row per pair with the number of documents in which
the pair co-occurs. By default every row of \code{data} is a document; give
\code{doc_id} to treat all rows sharing an id (a conversation, a user, a day) as
one document. The output mirrors \code{widyr::pairwise_count()} (\code{item1},
\code{item2}, \code{n}) and pipes straight into
\code{igraph::graph_from_data_frame()}, tidygraph or ggraph.
}
\details{
Glyphs are canonicalised through the package's codepoint key, so qualified
and unqualified forms of the same emoji (with/without \code{U+FE0F}) count as one
node. Pairs are between \emph{distinct} emoji: repeats of the same emoji in a
document do not pair with themselves (see \code{\link[=emoji_cooccurrence]{emoji_cooccurrence()}} for the
diagonal).
}
\examples{
df <- data.frame(text = c("fun \U0001f602\U0001f60d",
                          "\U0001f602\U0001f60d\U0001f389",
                          "just \U0001f602"))
emoji_pairs(df, text)
emoji_pairs(df, text, directed = TRUE)
}
\seealso{
\code{\link[=emoji_cooccurrence]{emoji_cooccurrence()}} for the same counts with an optional
diagonal; \code{\link[=emoji_ngrams]{emoji_ngrams()}} for consecutive sequences.
}
