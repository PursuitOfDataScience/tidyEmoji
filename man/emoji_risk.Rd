% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emoji-ambiguity.R
\name{emoji_risk}
\alias{emoji_risk}
\title{Interpretation risk per row}
\usage{
emoji_risk(data, text, measure = "entropy", threshold = NULL)
}
\arguments{
\item{data}{A data frame or tibble containing a text column.}

\item{text}{The text column to scan, supplied unquoted.}

\item{measure}{Ambiguity statistic to use; see \code{\link[=emoji_ambiguity]{emoji_ambiguity()}}.}

\item{threshold}{Value at or above which a glyph counts as ambiguous.
\code{NULL} (default) uses the lexicon's upper quartile of \code{measure}.}
}
\value{
\code{data}, as a tibble, with added columns \code{.emoji_n},
\code{.emoji_n_scored}, \code{.emoji_ambiguity_mean}, \code{.emoji_ambiguity_max} and
\code{.emoji_n_ambiguous}. Rows with no emoji get \code{NA} throughout.
}
\description{
\code{emoji_risk()} scores how likely each row's emoji are to be \emph{misread}, using
the annotation-disagreement statistics of \code{\link[=emoji_ambiguity]{emoji_ambiguity()}}. It is the
content-QA counterpart of \code{\link[=emoji_sentiment]{emoji_sentiment()}}: a row can carry a confident
positive score built entirely out of glyphs its annotators fought over.
}
\details{
\code{threshold} decides what counts as an ambiguous glyph for
\code{.emoji_n_ambiguous}. The default, \code{NULL}, uses the upper quartile of the
chosen measure across the whole lexicon, i.e. "in the most-disputed quarter
of all emoji". Supply your own number to make the cut-off explicit in your
script.

Emoji absent from the lexicon cannot be scored and are excluded from the
means; \code{.emoji_n} and \code{.emoji_n_scored} together show how much of the row
was actually measured.
}
\examples{
df <- data.frame(text = c("thanks \U0001f643", "great \U0001f600", "plain"))
emoji_risk(df, text)
}
\seealso{
\code{\link[=emoji_ambiguity]{emoji_ambiguity()}}, \code{\link[=emoji_flag_ambiguous]{emoji_flag_ambiguous()}}.
}
