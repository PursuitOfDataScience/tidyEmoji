% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emoji-time.R
\name{emoji_unicode_releases}
\alias{emoji_unicode_releases}
\title{Unicode and Unicode Emoji release dates}
\usage{
emoji_unicode_releases()
}
\value{
A tibble with columns \code{version} (character, the normalised label with
any leading \code{E} removed), \code{version_num} (the same parsed as a number, for
ordering), \code{series} (\code{"emoji"} or \code{"unicode"}) and \code{release_date} (a
\code{Date}).
}
\description{
\code{emoji_unicode_releases()} returns the publication date of each Unicode
Emoji (UTS #51) data-file release, plus the earlier Unicode versions that
introduced emoji before the emoji series was numbered separately. It is the
lookup that turns the \code{version} carried by the emoji reference table into a
date, and hence into a time axis.
}
\details{
Two numbering series exist and both turn up in emoji reference data. The
Unicode Emoji series (\code{series = "emoji"}) runs 1.0, 2.0, ... 5.0 and then
jumps to 11.0 to line up with the Unicode version; the Unicode series
(\code{series = "unicode"}) covers the 6.0-10.0 releases that added emoji before
the alignment. The two do not collide, so \code{version} is a unique key.

The table is kept in code rather than as a bundled \code{.rda}: it is a few dozen
rows, it changes only when Unicode ships, and keeping it beside the verbs
that use it means it can never drift out of sync with them.
}
\examples{
emoji_unicode_releases()
}
\seealso{
\code{\link[=emoji_version_profile]{emoji_version_profile()}} and \code{\link[=emoji_adoption_lag]{emoji_adoption_lag()}}, which join to
this table; \code{\link[=emoji_unicode_version]{emoji_unicode_version()}} for the version this build reflects.
}
